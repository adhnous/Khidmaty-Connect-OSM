# --- Next.js App Env (client-side vars must be prefixed with NEXT_PUBLIC_) ---
# Map provider (keep 'osm' for free maps)
NEXT_PUBLIC_MAP_PROVIDER=osm
# Optional: override OSM tiles (keep defaults if unsure)
# NEXT_PUBLIC_OSM_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
# NEXT_PUBLIC_OSM_ATTRIBUTION=&copy; OpenStreetMap contributors

# Google Maps (leave empty to stay 100% free)
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Firebase (client config)
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyDr43GAZFdLh674vZOzlXR_OawyFP0arRY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=khidmaty-connect-2d512.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=khidmaty-connect-2d512
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=khidmaty-connect-2d512.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=587434148277
NEXT_PUBLIC_FIREBASE_APP_ID=1:587434148277:web:1a1aeec6f34435023fd9fc

# Optional: override the embedded Python REPL (JupyterLite) used in /python-sandbox
# Example (your own deployment):
# NEXT_PUBLIC_PYTHON_REPL_URL=https://your-python-lab.example.com/repl/index.html?kernel=python&toolbar=1
NEXT_PUBLIC_PYTHON_REPL_URL=

# Firebase Admin (server-only for analytics/events)
# If using a service account, escape newlines in private key as \n
# FIREBASE_PROJECT_ID=khidmaty-connect-2d512
# FIREBASE_CLIENT_EMAIL=service-account@khidmaty-connect-2d512.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Image uploads (choose one: firebase | cloudinary | local | inline)
# NEXT_PUBLIC_IMAGE_UPLOAD_MODE=inline
# Cloudinary (only if using cloudinary mode)
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# NEXT_PUBLIC_CLOUDINARY_PRESET=

# Genkit (optional AI flows)
# GOOGLE_API_KEY=

# Dev-only: enable the one-time Google OAuth token exchange helper route
# at /api/google/oauth/callback (do NOT enable in production).
# ENABLE_GOOGLE_OAUTH_HELPER=1

# --- Public Analytics Dashboard (Looker Studio / Grafana public embed) ---
# Paste a public iframe URL here; page will render it at /analytics/public
NEXT_PUBLIC_PUBLIC_DASHBOARD_URL=

# --- Metabase Signed Embed (private per-user dashboards) ---
# Base URL of your Metabase instance (no trailing slash), e.g. https://metabase.yourdomain.com
METABASE_SITE_URL=
# JWT signing secret from Metabase Admin > Settings > Embedding in other applications
METABASE_JWT_SECRET=
# Numeric dashboard ID to embed
METABASE_DASHBOARD_ID=
# Optional embed styling
METABASE_EMBED_THEME=night
METABASE_EMBED_BORDERED=false
METABASE_EMBED_TITLED=false

# --- Contact form basic rate limiting (server-only) ---
# CONTACT_RATE_LIMIT_MAX=5
# CONTACT_RATE_LIMIT_WINDOW_MS=3600000
# CONTACT_RATE_LIMIT_SALT=change-me

# --- Student Bank PDF storage (MinIO / S3) ---
# Server-side only. Objects are kept private and served via short-lived signed URLs.
S3_ENDPOINT=
# Optional: public endpoint (browser-reachable) used when generating presigned URLs.
# If your site is https, this should be https too.
S3_PUBLIC_ENDPOINT=
S3_REGION=us-east-1
S3_FORCE_PATH_STYLE=true
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
# Optional (default cloudai-student-resources)
S3_BUCKET_STUDENT_RESOURCES=
